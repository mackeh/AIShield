id: AISHIELD-KT-AUTH-004
title: Hardcoded Bearer Header Assembly
severity: medium
confidence_that_ai_generated: 0.70
languages: [kotlin]
category: auth
pattern:
  all:
    - "authorization: bearer"
    - "+ userinput"
fix:
  suggestion: Build authorization headers from runtime secrets and avoid hardcoded bearer patterns.
tags: [auth, secrets]
