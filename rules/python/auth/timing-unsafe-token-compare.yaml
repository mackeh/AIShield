id: AISHIELD-PY-AUTH-001
title: Timing-Unsafe Secret Comparison
severity: high
confidence_that_ai_generated: 0.88
languages: [python]
category: auth
ai_tendency: LLM output frequently uses simple equality checks for auth tokens.
pattern:
  any:
    - "token == "
    - "api_key == "
  not:
    - "compare_digest("
fix:
  suggestion: Use hmac.compare_digest() for secret or token comparisons.
tags: [auth, timing-attack]
