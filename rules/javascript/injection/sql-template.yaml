id: AISHIELD-JS-INJ-001
title: SQL Query Built with Template String
severity: high
confidence_that_ai_generated: 0.80
languages: [javascript]
category: injection
ai_tendency: AI frequently uses interpolation for SQL because it reads naturally.
pattern:
  contains:
    - "query(`select"
    - "query(\"select \" +"
    - "query('select ' +"
negative_patterns:
  - "query(\"select * from users where id = ?\""
fix:
  suggestion: Use parameterized queries and avoid interpolation for user-controlled values.
tags: [injection, sql]
