id: AISHIELD-DOCKER-MISC-007
title: Secret Value in ENV Instruction
severity: high
confidence_that_ai_generated: 0.82
languages: [dockerfile]
category: misconfig
cwe_id: CWE-798
owasp_category: A07:2021 - Identification and Authentication Failures
ai_tendency: AI-generated Dockerfiles often set secrets as ENV variables baked into image layers.
pattern:
  all:
    - "env "
    - "password"
fix:
  suggestion: Pass secrets at runtime with --env or use Docker secrets / build-time secrets (--mount=type=secret).
tags: [dockerfile, secrets, misconfig]
